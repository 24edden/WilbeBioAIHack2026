@AGENTS.md

## Claude Code

The project instructions above are imported from `AGENTS.md`, which Codex and other
agents read directly. Put shared instructions there, not here. This file is only for
things specific to Claude Code.

- Pull a context file into a single prompt with `@`, e.g.
  `@Context/judgingCriteria.md does this plan score well on execution?`
