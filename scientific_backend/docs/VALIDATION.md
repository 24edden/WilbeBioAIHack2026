# Validation record — 20 September 2026

## Expanded reasoning, attributable skills and Ana paired-study integration

The current release raises actual per-response allowances to 24,000 specialist and 48,000 synthesis/reviewer tokens, preserving Astra/high and advisory aggregate budgets. The scientific role loop loads the pinned Team TBD Rosalind-informed workflow skill. Public sequence discovery uses the actual installed OpenAI UniProt and RCSB PDB instructions/client scripts plus molecular/BioNeMo guidance, with independent model review and immutable source-qualified drafts. The website separates applied receipts, installed availability and actual model identity.

The new Ana input is `/home/ubuntu/ana-workspace/datasets/agent_access/GSE28460`, symlinked from Ana's input directory. It was absent from the previous run and catalog. Exact eight-file pins now register 49 diagnosis/relapse pairs (98 arrays) after conventional ALL treatment, explicitly distinct from the CD19 CAR-T study. Historical/evaluator outputs were not used. The deterministic paired analysis verifies sample identities, existing log2 scale and probe mappings, tests curator-supplied panels separately from the scientist's original hypothesis, and retains full downloadable gene results. Scientist-required analyses execute and pass acceptance before any research-model call.

The live public-source smoke check made four successful requests through the pinned installed scripts: RCSB 7URV entry and two entities, plus UniProt P15391. It verified the 257-residue deposited CD19 construct, 242-residue FMC63 scFv and 556-residue canonical CD19 sequence; it did not select a candidate design or run NVIDIA. Exact raw-response hashes/receipts are in private sequence-source runtime. The final combined local suite passed **742 tests**, including all 16 required-analysis acceptance, provenance and downloadable-artifact checks. Deployment and live-investigation receipts follow after execution.

The expanded deployment passed **707 tests on Brev**, with 35 Node-dependent renderer tests skipped there and passed locally. The private installed OpenAI source-client runtime and all skill hashes verified. Actual Ana inputs passed their pins, pairing and deterministic analysis preflight. Source release `60b00ab9711f316f25babea31a79efd818a3bafb93b0418807c18eebc9d8959f` restarted healthy, preserving a before/after digest of every existing run and action. Backups use `runtime/pre-science-expansion-20260920` with SQLite, source archive and private environment variants; credentials never leave the private host. The effective service settings are 24,000 specialist / 48,000 synthesis tokens, 2,000,000 observed input / 300,000 output advisory thresholds, 120 requests and 180 tools. This receipt was added after source packaging.

Fresh live run `037a6844bfb54d5a8d9010043d6c0a89` was requested at 10:00:52 UTC on 20 September with `gse28460-paired-expression` required before model dispatch. The exact original user hypothesis and source identity match `b5898d6c3b3940ee9eac90f01b8b8e19`; prior decisions are preserved in their original run. Final live scientific and sequence-discovery receipts follow after completion.

### Expanded evidence and accepted-analysis continuation checks

The full combined local suite now passes **776 tests**. An expanded input packet is allowed up to 300,000 serialized characters; oversized, duplicate-ID and missing-ID inputs still fail before model transport. Required analyses already executed and accepted must be read by bioinformatics after source/hash validation, rather than redundantly executed to satisfy a tool-use gate. Ordinary investigations retain their execution requirement. Interpretation now requires substantive findings for all accepted scientist-required analyses, with a separate AI-review check of result, cohort scope and decision relevance; required-operation receipts are pinned at selection.

The first live decision completed with 32 Astra/high requests (1,437,514 input / 43,911 output tokens), 9 accepted handoffs and 44 skill receipts. Its reviewer requested and received the PTBP1 measurement replay before publication, then selected the NVIDIA exon-2 structure follow-up autonomously. Both 272/183-residue Boltz2 requests completed and their accepted evidence was retained. The next model reassessment encountered the older 150,000-character input guard at 156,702 characters, before constructing any model transport. That known failure has empty provider metadata and no unknown actions. The guarded capacity deployment preserved all run/action records; release `268073362658483cae19e4a7c90ad9f32605ce125da73a28d48208bccf74a7f9` passed 747 local and 712 staged Brev tests (35 renderer tests passed locally).

An explicit recorded operational reassessment then made 4 completed Astra requests, but bioinformatics correctly reused saved analyses and the old tool-use gate rejected the absence of redundant execution. This failed attempt and its usage remain recorded; no ambiguous provider call or discarded scientific decision is relabeled successful. The corrected gate above addresses that live finding. Subsequent continuation and final interpretation receipts are recorded after completion. This is an operator-assisted recovery during evaluation, not evidence of an uninterrupted autonomous run.

The combined study-review release `7fbf61d72bdd2e904be0bf94ed1be0a4e42a4054aa9b60403e25ebdf45331668` passed **741 staged Brev tests**, with 35 Node-dependent checks passed locally. The production service stayed available during staged tests and restarted only at the idle deployment boundary. Actual saved evidence passed both the required-analysis qualification and summary-coverage preflight; all run/action records and private configuration remained unchanged. Backups use `runtime/pre-study-review-20260920`. A new explicitly recorded feedback operation continues the original question under the corrected gate; its predecessor’s 4 completed calls and rejected handoff remain preserved. No NVIDIA resubmission was made by this correction.

### Completed expanded study, interpretation and live sequence discovery

Run `037a6844bfb54d5a8d9010043d6c0a89` completed its scientific reassessment at 10:39:20 UTC on 20 September, publishing decision v2 and 18 total accepted handoffs. The Ana cohort is explicitly represented as conventional-treatment relapse context. The final working answer favors altered CD19 RNA processing/surface availability within reporter and cell-system scope; it does not rank a patient's causes from unrelated cohorts. Governance stops at `wet_lab_required` and proposes a controlled target-by-CAR-product functional experiment. Independent scientific review found no blocking overclaim.

Two real Astra/high calls then wrote and reviewed the concise addendum, using 203,623 input / 15,978 output tokens and 48,000-token allowances. A substantive finding includes the required 49-pair GSE28460 analysis, its panel estimates and scope. The saved-artifact audit verifies both NVIDIA predictions and explains the 12.47 Å global versus 0.57 Å exploratory 46-position alignment sensitivity without claiming a validated confidence threshold or binding measurement.

AI sequence discovery completed at 10:47:26 UTC with eight actual Astra/high calls (261,826 input / 5,271 output tokens), 26 completed public-source request receipts and 14 applied-skill receipts. It used the actual pinned installed OpenAI UniProt/RCSB clients and preserved exact deposited sequence bytes. The independently reviewed result selects 7URV's 257-residue soluble CD19 and 242-residue complete FMC63 scFv. The result remains `needs_inputs`: examined alternative records contain separate Fab chains, not a qualified complete single-chain candidate. No candidate sequence, linker, patient construct identity or target retention was invented; no NVIDIA call was submitted by sequence discovery.

Final run usage is 68 model calls, 3,264,154 input and 108,815 output tokens, including the four calls in the preserved failed gate attempt. All eight discovery requests carry actual 24,000 specialist / 48,000 reviewer allowances and high reasoning. Offline export verification passed; comparisons preserve original hypothesis, both scientific decisions, accepted evidence, handoffs and governance byte-equivalent as objects after both addenda. Selected sequence hashes and all three downloadable GSE artifacts verified. The healthy private service reports the expanded settings and active installed skills. Twenty-five focused UI tests plus a combined saved-brief/discovery refresh exercise verify auto-population, edit preservation and unchecked retention; no actual-browser visual pass is claimed.

See [the complete handoff and receipts](../../outputs/team-tbd-ana-sequences-20260920/README.md). The source release and scientific records remain distinct from these subsequently written validation receipts.

## Clear interpretation and existing-artifact molecular audit

The [research interpretation release](RESEARCH-INTERPRETATION.md) passed **585 offline tests locally**, JavaScript/Python syntax and all pinned skill hashes. New checks cover a typed model-written working answer and independent AI review, source/sequence-role validation, immutable addendum publication, idempotent selection, cancellation, restart, unknown outcomes, cached-success recovery, accurate UI reuse labels and safe form preparation. Both coordinator/reviewer and relevant specialist contexts load the new versioned interpretation guidance. Historical decisions and governance are not rewritten.

The [molecular audit](MOLECULAR-INTERPRETATION.md) reads the saved CIFs and existing request receipts. The local export verifies 272/183-residue target monomers, reproduces the 12.47 Å all-shared-residue comparison and records post hoc local-metric/alignment sensitivity. The fractional raw local metric encoding is flagged as ambiguous; the selected 46-residue fit (0.57 Å) is not called a validated high-confidence core. No new NVIDIA prediction was made by these checks. Actual model generation and deployment receipts follow after live validation.

Deployment passed **565 offline tests on Brev**, with 20 Node-dependent browser-renderer tests skipped there and passed locally. Actual saved-artifact preflight verified both exact sequences and the bounded model context; submitted NVIDIA settings matched. The service restarted healthy with no active investigations, and before/after digests preserved all existing run bodies and action records. Release SHA-256: `c5a1150c684737ccb4ca85b4fe3e61103c5a9b915442ca33164f84bae0934d38`; backups: `runtime/pre-interpretation-20260920.sqlite` and `runtime/pre-interpretation-20260920-source.tar.gz`. This receipt was added after creation of that source archive.

## Plain-language refinement and first live interpretation

The first model-written interpretation completed with two actual GPT-6 Astra requests and independent AI acceptance: 169,535 input and 14,217 output tokens, with eight applied-skill receipts. Run `b5898d6c3b3940ee9eac90f01b8b8e19` retained its original hypothesis, three scientific decisions, accepted evidence, 27 handoffs, governance and original action records. Interpretation version 1 is a separately hashed addendum, not a fourth scientific decision or a retroactive governance promotion. Export verification passed. No additional NVIDIA call was submitted.

Independent scientific review found the first explanation faithful to the accepted results but too technical for the requested audience. The clarity refinement enforces concise narrative fields, separates hashes and internal labels from readable explanations, and requires independent review of plain language and decision relevance. Shared research-interpretation guidance advances to version 1.1.0. Verified molecular fields now prepare automatically only when the form is untouched; user edits and target-retention choices are preserved. The full refined local suite passed **601 tests**, including the browser-renderer checks, with one existing dependency deprecation warning. Live validation of the refined explanation is recorded separately below.

The refined deployment passed **578 offline tests on Brev**, with 23 Node-dependent renderer tests skipped on that host and passed locally. Source release `3173e40fc2bf8a29151aa738aaf19cc74e688d1100cf93ed4d10c19fd63994b2` preserves all stored run bodies and actions. Backups are `runtime/pre-interpretation-clear-20260920.sqlite` and `runtime/pre-interpretation-clear-20260920-source.tar.gz`; the private service restarted with a healthy worker and no active investigations. This receipt was added after creation of that release archive.

The browser's security-policy check was unavailable during this session, so no actual-browser visual pass is claimed for this release. Existing server workflow and isolated renderer tests validate functionality; the earlier browser controls were left unchanged.

### Completed plain-language interpretation, version 2

At **09:29:00 UTC on 20 September 2026**, operation `e4b3b09f65fb45b88224eb8823fe00da` completed on the existing live run. Two actual GPT-6 Astra/high requests used **170,309 input and 13,058 output tokens**, with zero acceptance-repair calls and eight applied-skill receipts. The independent model reviewer accepted all eight checks, including plain language and decision relevance. A separate read-only scientific review found no blocking error and judged clarity materially improved; minor specialist phrasing remains. This is AI review, not human scientific approval.

The published working answer identifies altered CD19 RNA processing as the strongest measured target-side lead while keeping splice mediation, CAR recognition and patient attribution unproven. The NVIDIA section explains that selected shared regions align closely and large overall displacement does not establish loss of the entire remaining fold; confidence encoding remains ambiguous. Both exact target sequences are shown. The molecular draft selects the verified reference target and leaves both unprovided binder sequences absent. The proposed next experiment crosses tumour targets with patient/reference CAR-T cells and tests whether correcting CD19 processing restores recognition; it was not executed.

Version 1 remains unchanged. Automated comparisons confirmed preservation of the original hypothesis, three scientific decisions, accepted evidence, 27 handoffs, governance history, follow-ups and prior action records. The final export verified both addenda and all three source decisions with zero external calls. No new NVIDIA requests, whole-team rerun or raw-data reanalysis occurred during interpretation. The private localhost tunnel and healthy service were checked after completion.

Final artifacts are [the version-2 brief](../../outputs/team-tbd-interpretation-20260920/working-answer.json), [complete verified export](../../outputs/team-tbd-interpretation-20260920/run-export.json) and [research report](../../outputs/team-tbd-interpretation-20260920/run-report.md). Version-1 copies are retained alongside them.

## Local demo connection recovery

The Mac's loopback 8081 listener was absent while the Brev application remained healthy with zero active investigations. Restored private forwarding, verified the page/health/completed `b5898d6` run, then transitioned only that forward to the reconnecting local helper documented in [the Brev runbook](BREV.md). The shared SSH master and remote service were not stopped. The managed supervisor reported `forwarding`; the application remained healthy and the completed run retained three decisions and 27 handoffs. No scientific operation or inference was submitted.

Seven focused offline tests cover reconnect, stale generation rejection, safe shutdown, duplicate prevention, occupied-port preservation, private state and connection settings. The full local suite passed **514 tests** with one existing dependency warning. The helper reconnects dropped SSH sessions while it remains running; it does not install an OS startup service or promise availability after a Mac reboot.

## Completed live governance loop — new data and agent-selected NVIDIA analysis

Run `b5898d6c3b3940ee9eac90f01b8b8e19` completed at **01:41:52 UTC on 20 September**, following the explicit checkpoint recovery documented below. It published **three immutable AI-reviewed decisions, 27 accepted handoffs and 37 stage evaluations**. The handoffs include 24 model-driven products and three explicit pharmacology prerequisite blocks. Top-level human review remains `unreviewed`.

The independent model reviewer selected two successive follow-ups with origin `agent_governance`: first `cd19-ptbp1-surface-qpcr`, then `cd19-exon2-structure`. Both executed, their results were accepted and the full specialist team reassessed the new evidence. The operator did not force either selection. The second follow-up produced two actual NVIDIA `mit/boltz2` HTTP 200 responses for reference CD19 (272 residues) and an exon-2-deleted construct (183 residues), request IDs `07f57391-aa22-412f-8735-0420c6e1b0de` and `9444a7f3-4d4c-46b8-a49c-7e5d92891e6f`. Those predictions are exploratory monomer geometry, not a measured binding or recognition result. Actual OpenAI model identity remains **GPT-6 Astra, high reasoning**, the authorized placeholder; this run does not demonstrate GPT-Rosalind inference.

The final ledger keeps the original patient-level hypotheses and the separately attributed computational alternative `possible`. It does not force a promotion or exclusion simply because more work was performed. Final stop reason is **`needs_method`**: the team requests a regional-confidence, request-settings and alignment-sensitivity audit of the existing NVIDIA artifacts, for which no registered diagnostic recipe exists. It explicitly declines to repeat the completed prediction. A matched tumour-target × patient/reference CAR-T functional experiment remains proposed, not performed; additional public-data analysis also remains qualified future work.

The final export passed the offline checksum, original-hypothesis, decision-version and claim-reference verifier with three decisions and zero external calls. Final decision v3 SHA-256: `17df3de3c6ee7766966cd1ac9d4031f0f4ed5bfd71cc6dd996cd10d8b9b0c797`. See the [saved export](../../outputs/team-tbd-five-minute-demo-20260920/evidence/run-export.json), [research report](../../outputs/team-tbd-five-minute-demo-20260920/evidence/run-report.md) and [independent scientific/management review](../../outputs/team-tbd-five-minute-demo-20260920/review/FINAL-MANAGEMENT-SCIENCE-REDTEAM.md). The review supports a bounded, paper-informed workflow demonstration and partial reproduction; patient-level causation, full-paper reproduction, optimal planning and human scientific approval are not established. Presentation/video production is separate from these runtime checks.

## Known incomplete synthesis: capacity and explicit checkpoint recovery

Replacement investigation `b5898d6c3b3940ee9eac90f01b8b8e19` reached coordinator request 27, which returned HTTP 200 with a response ID, `status: incomplete` and `reason: max_output_tokens`. Its 8,000-token allowance was exhausted; seven specialist handoffs were accepted and no decision was published. This is a known incomplete result, unlike the earlier request with an unknown outcome. Neither attempt is a completed scientific assessment.

The [synthesis recovery release](SYNTHESIS-RECOVERY.md) gives coordinator/reviewer a separate default 24,000-token allowance and adds an explicitly selected, server-validated checkpoint operation. It preserves the seven original products and failed request, runs fresh coordinator/reviewer calls and records exact reuse lineage. Unknown outcomes, missing or altered products, changed source/skill/evidence versions, forged client checkpoint data and stale selection are rejected. Checkpoint synthesis cannot execute new diagnostics; any selected new analysis goes through governance and the full specialist cycle after publication.

A read-only exported copy of the actual `b5898d6` run passed the checkpoint's source, skill, evidence and handoff validation under the same configured Astra model. This preflight made no provider call and changed no run. It does not substitute for the transactional action-journal check at live selection or for successful live synthesis.

**507 offline tests passed locally** after all recovery changes were frozen. Coverage includes 29 checkpoint/lifecycle tests, 15 synthesis SDK tests over mocked HTTP, 14 API/browser tests and capacity checks in the provider suite. It verifies only fresh coordinator/reviewer dispatch during recovery, unchanged specialist products/proposals/evidence, role-specific output allowances and repairs, input qualification, new acceptance lineage, explicit same-key retry without duplicates, cancellation/restart handling, read-only evidence constraints and truthful reused-work UI. JavaScript syntax passed. Independent review found no blocker in this narrow recovery path. No live continuation was submitted by these implementation checks.

Deployment passed **497 offline tests on Brev**, with ten Node-dependent renderer tests skipped there and passed locally. Skill integrity and the actual action-journal preflight passed. The service restarted healthy with zero active investigations. Before/after digests confirmed **all stored run bodies and action records** unchanged. Source archive SHA-256: `e27354434f96bf02579704e100d80c0ef4af4fdf406eaf384066602c4d498baa`; backups: `runtime/pre-synthesis-20260920.sqlite` and `runtime/pre-synthesis-20260920-source.tar.gz`. This deployment receipt was added after creation of that archive.

Read-only localhost verification at 01:13 UTC on 20 September confirmed the worker alive, advisory budgets, 8,000 specialist / 24,000 synthesis output allowances and 600-second request / 1,800-second agent waits. The readiness API accepted `b5898d6` with checkpoint `synthesis-checkpoint-9ac5ef318e20e47eb14fc356` and source operation `a8c40f16ca0740d28a22779ec82b1e46`, and rejected the historical unknown-outcome `5c3ab943` attempt. These implementation/deployment checks submitted no live continuation; the separate presentation task owns its explicit invocation and live behavioral evaluation.

## Coordinator timeout and supported recovery

Fresh investigation `5c3ab9433a454d1d82d80dea00a925bc` preserved seven accepted handoffs and newly computed clinical-pair/PTBP1 evidence, but published no decision. Request 28 (coordinator) was dispatched without a response ID or complete response; the approximately 120-second interval matches the prior hard-coded transport wait. Its outer action remains `unknown`. This is an interrupted live attempt, not a completed investigation or a successful autonomous continuation demonstration.

The runtime correction replaces fixed 120-second request and 240-second ordinary-role limits with validated session settings: 600-second request wait and 1800-second enclosing role wait by default, with a 15-second connection limit. Finalization/format repair use request wait plus 30 seconds. Model choice, reasoning effort, output allowance, scientific methods, skill versions and action authorization are unchanged. Retries remain disabled; request timeouts retain unknown status and now get a clear public message and safe structured failure metadata.

**445 offline tests passed locally**, including 25 added timeout contracts over the actual Agents/OpenAI SDK with mocked HTTP. They cover settings validation before transport, stable session values, request timeout extensions, every role's enclosing wait, finalization/format repair, one request on timeout, unknown durable action state and preservation of accepted evidence. Independent review found no remaining fixed 120-second limit in the investigation paths. The capability probe retains its separate shorter bounds.

The blocked live attempt has no supported in-place checkpoint resume: accepted work products are durable audit records, not a saved model continuation. Preserve the old run unchanged and use an explicit new investigation with a new request key, identified as a replacement attempt. A new run does not resolve the historical unknown response. This patch makes no claim that longer waits guarantee model completion.

Deployment passed **441 offline tests on Brev**, with the same four Node-dependent renderer tests skipped there and passed locally. The service was backed up and restarted healthy with zero active runs. Before/after digests verified **all stored run bodies and action records** unchanged, including the interrupted run's handoffs, evidence and unknown request. Source archive SHA-256: `279b118b55adc60011954f4b8a8f96f824c4b5fa2430231f34b40115aa4a9358`; backups: `runtime/pre-timeout-20260920.sqlite` and `runtime/pre-timeout-20260920-source.tar.gz`. This deployment receipt was documented after creation of that archive. No new provider call was made during the correction's software/deployment checks.

## Hypothesis governance and autonomous continuation — offline validation

The [hypothesis-governance release](HYPOTHESIS-GOVERNANCE.md), process contract `team-tbd-resistance-4`, passed **420 offline tests locally** on Python 3.13. This integrated suite also includes the two newly registered public-source clinical-pair and PTBP1 surface/qPCR recipes. One existing Starlette/AnyIO deprecation warning remains. All pinned skill hashes passed integrity checks and the browser script passed its syntax check.

The governance checks include 72 evidence/state/history contract cases, 31 continuation and recovery cases, five actual Agents SDK integration cases over mocked HTTP, and eight API/report/browser-renderer cases. They exercise successive reviewer-selected analyses and reassessments, explicit unresolved stops, atomic publication and queuing, original hypothesis and decision preservation, source qualification, same-input NVIDIA reuse, rejection of unknown/failed automatic replay, cancellation, restart recovery, model-context attribution and visible execution state. These are engineering checks; a cited evidence record or valid state transition does not prove a scientific interpretation.

All nine scientific role instructions and the shared discovery-planning skill now require scoped testing, meaningful falsification and a useful next test or explicit blocker. Existing historical runs retain their pinned policy; they are not retroactively opted into autonomous continuation. No new live investigation, model inference or NVIDIA submission was performed for this implementation validation. The separately requested new demo run must supply the live behavioral and scientific evaluation.

The combined release passed **416 tests on Brev** (Python 3.12), with four browser-renderer tests skipped because Node is absent on that host; all four passed locally. Pinned skill integrity passed on Brev. The idle private service was backed up, updated and restarted healthy with zero active investigations. A before/after digest confirmed all stored hypotheses, decision histories and usage records were unchanged. Deployed source archive SHA-256: `5742656db38e4b352e508a7b4932d49263b48b31adf3911eb79e493aa477db0c`. Backups: `runtime/pre-governance-20260920.sqlite` and `runtime/pre-governance-20260920-source.tar.gz`. This deployment receipt was added to the documentation after that source archive was created.

## Discovery planning and BioNeMo awareness — no live rerun

The [discovery-planning release](DISCOVERY-PLANNING.md) passed **296 offline software tests locally** on Python 3.13 and **296 on Brev** on Python 3.12. This includes the actual Agents SDK over mocked HTTP transport: earlier catalog awareness, source-qualified specialist proposals, accepted handoff propagation, retention through an unrelated content repair, isolation after ultimate rejection, forged identity/citation rejection and no executor or NVIDIA calls from planning tools. The worker test checks persisted shared-skill receipts and the bioinformatics-to-molecular dependency. One pre-existing Starlette/AnyIO deprecation warning remains.

The nine scientific role instructions were versioned, a shared planning instruction was added, and all manifest hashes were verified. The attributed NVIDIA skill bytes are unchanged; permission to load them extends to relevant computational/interpretation roles. This release changes planning and handoff behavior, not model entitlement, installed NVIDIA services or follow-up execution authority. No new live investigation or provider inference was performed for this release. Whether a real model chooses useful follow-ups under these instructions still requires a separately requested live evaluation. Historical live receipts below refer to earlier instruction versions. The idle private service was backed up and restarted successfully; the before/after digest of all existing hypotheses, decision histories and usage records was unchanged. Skill hashes verified on Brev, and the restarted worker reported zero active investigations. Release archive SHA-256: `d4ab4c72db48b75947f6e521ac008b4410b44d757dadddc603b03a2e8e615d17`.

## Scientific answer red-team and fresh live rerun

See [the scientific red-team review](SCIENTIFIC-REDTEAM.md) for the reasoning criteria, discovered weaknesses, instruction and report corrections, independent verdicts and remaining data gap. Fresh run `a90a027775ad4342a7bc32620e6174f2` completed two live specialist/reviewer cycles and a real two-request NVIDIA follow-up. It used 51 successful Astra requests, with 18 accepted handoffs and two immutable decisions. Decision 1 and the original hypothesis were preserved exactly.

All 249 software tests passed locally and on Brev after the changes. The final export passed content/citation verification, all request usage totals reconcile, and the Markdown export includes all recorded alternatives. Independent reviewers judged the revised answer suitable as an honest public-data research demo. This is not general scientific validation or proof of a patient-specific cause. The separate CAR-T Seurat cohort remains unanalyzed by the current tools.

## Scientific correction after the first completed run

The first completed run correctly counted 195 RNA barcodes absent from the mutation table, but its recommendation to treat them as unresolved variant identities was incomplete. The public RNA table marks **all 195 as unmutated wild-type controls** (`mutations='-'`); there are 195 such rows in replicate 1 and 194 in replicate 2, consistent with the published methods. A variant-only table naturally omits these controls. The public study is [Cortés-López, Schulz, Enculescu et al., Nature Communications 2022](https://pmc.ncbi.nlm.nih.gov/articles/PMC9500061/).

Historical run `a01769d0494a48fe84a2805208366b2e` and its original export remain immutable. The new discovery recipe uses the source control annotation and author exon definitions; new investigations pin these additional reference versions. Software acceptance of the earlier run did not establish the correctness of its biological interpretation.

## Discovery-release software checks

The source-qualified CD19 discovery, follow-up actions and actual-coordinate preview release passed 249 tests locally and on Brev. Three generated public-evidence packs passed manifest reproducibility checks. The scientific recipe was independently recomputed and audited: 195/194 explicitly annotated controls, 9,321 shared barcodes, 56 direct single-mutant constructs and 30 replicated cryptic associations. Eight direct constructs meet an exploratory empirical-control-range criterion; this is not a significance count. A746G exon-2 skipping is 339/375 and 942/1022 reads in the same construct across two replicates, conditional on the paper’s engineered G748T assay baseline. See [method, exact inputs and limitations](CD19-DISCOVERY.md).

The full optional softmax fit has not completed on the public dataset. It is excluded from initial-agent and reviewer-diagnostic tools, but remains an explicitly selected follow-up with convergence reporting.

## Completed public-data discovery and NVIDIA follow-up

Run `ff3c4fd47f1d408ab5bcdc89d1a07744` preserved the original CD19 hypothesis byte-for-byte and was started through the live website using the updated source snapshot. The initial investigation completed with 26 actual Astra requests, 296,382 input tokens and 22,770 output tokens. It accepted the freshly computed barcode QC, isoform summary and source-qualified variant-to-splicing discovery. The independent reviewer refined the requirement to reconstruct A746G on both native and engineered backgrounds. No extra reviewer diagnostic was executed in that first operation.

The website's **Run NVIDIA structure** action selected the registered `cd19-exon2-structure` recipe against decision v1. This action was a scientist-selected registered option; the model separately recommended optional softmax analysis. Two actual hosted Boltz-2 requests returned HTTP 200 and validated 272- and 183-residue structures. Prediction evidence `NVIDIA-CD19-d0f1533df0693f10` was accepted before a second specialist/reviewer pass. Decision v2 published at `2026-09-19T20:13:17.263855+00:00`; the original v1 remains unchanged.

The completed run contains 2 decisions, 18 accepted handoffs, 24 recorded stage evaluations, 24 applied-skill receipts and 9 accepted evidence records. Settled usage is 48 actual research-model requests, 678,747 input tokens, 48,332 output tokens and 47 worker-recorded tool events. NVIDIA requests are recorded separately. Actual returned research-model identity is `gpt-6-astra`; this is not a GPT-Rosalind inference claim.

Both prediction artifacts and previews were verified through the deployed API and displayed in the actual browser. An independent reader rechecked exact sequence mapping, all atom coordinates, 2,120/1,433 atom counts and file hashes. The 183-position backbone RMSD reproduced at 12.470841970891577 Å with the same parser precision. See [provider requests, hashes and scope](NVIDIA-STRUCTURAL-FOLLOWUP.md).

The export passed `python -m app verify-export` with two versions and zero external calls during verification. The final scientific decision prioritizes an independently reconstructed, surface-density-controlled isoform-to-recognition experiment. Reporter splice effects and monomer geometry do not establish a particular patient's resistance mechanism. No experimental measurement was fabricated, no wet-lab experiment was executed, and the optional full softmax fit remains unperformed.

## Observed checks

| Check | Observed result |
| --- | --- |
| Local Python 3.13 regression suite, discovery release | 249 passed; one dependency deprecation warning |
| Brev Python 3.12 regression suite, discovery release | 249 passed; one dependency deprecation warning |
| Exact Brev OpenAI key | Authenticates; lists GPT-6 Astra; no GPT-Rosalind model listed |
| GPT-Rosalind API attempt | `gpt-rosalind-research` returned HTTP 404 `model_not_found`; access remains unavailable |
| Authorized Astra-high call | Successful real Responses request; returned model `gpt-6-astra` |
| Actual Agents SDK capability probe | Two real Astra requests, function-tool round trip, typed output validation; 365 input / 70 output tokens |
| Private Brev service | Team TBD on loopback 8080, healthy worker; private Mac tunnel 8081 |
| Live catalog and skill APIs | 21 dataset groups and 10 registered scientific skills returned |
| Actual HDF5 computation | LEON-BCMA `sample_06.h5`: 33,538 features, 5,167 barcodes, 3,296,324 sparse entries scanned |
| Actual Matrix Market computation | GSE234261 sample 25: 36,665 features, 1,559 barcodes, 2,661,860 sparse entries scanned |
| Matrix source integrity | Both selected inputs and companions hash-verified; CD19/TNFRSF17/CD3D queries completed without allocating a dense expression matrix |
| First broad real model investigation | Selected six studies and accepted 15 computed evidence records, then exhausted the observed input budget before a specialist handoff. Saved honestly as failed; no final scientific conclusion issued |
| Actual NVIDIA service inference | One hosted Boltz-2 request returned HTTP 200 and a validated 110-residue monomer, 838 atoms; official engineering sample only. CAR-T matched comparison remains unverified |
| Earlier browser workflow | Real backend source selection, evidence, correction/outcome versions, export and reload exercised; synthetic software-test measurement labeled explicitly in isolated runtime |
| Latest actual browser inspection | Verified blank intake, disabled empty submission, custom BCMA wording preserved across collection changes, explicit CD19 example selection and actual live submission. Visible role progression, scientific handoffs, acceptance checks and separate NVIDIA output panel verified in Chrome |
| Completed real browser-started investigation | `a01769d0494a48fe84a2805208366b2e`: nine accepted role handoffs, 14 accepted stage evaluations, five evidence records and one published decision; actual model identity `gpt-6-astra` |
| Real independent review loop | Reviewer requested `cd19-barcode-qc`; computed evidence was accepted before reassessment. The final decision added exclusion/repair of 195 unmatched RNA barcodes while retaining an inconclusive scientific assessment |
| Completed-run export | JSON content, original hypothesis, decision version/hash and evidence references passed `python -m app verify-export`; zero external calls during verification |

The real NVIDIA service check `bionemo-monomer-20260919-5a38419a` returned request ID `51bed1f8-3e19-47a3-bdab-37d4b3fef082`. Its structure hash is `8d97359361dcadb273b2f61de819a6025d7595dfee49f5a9544efdcde356e1fa`; confidence 0.4335459768772125 is a prediction metric, not measured biological performance. Exact sequence, one chain, complete alpha-carbon coverage and finite coordinates passed. See [service-check receipt and scope](BIONEMO-SERVICE-CHECK.md). It did not test a CAR-T mechanism.

The matrix validation receipt is stored on Brev at `runtime/data-validation.json`, SHA-256 `420bd8abddbabc6f439ceab6216a394790847c77848f29e33939e7e1eddef941`. These descriptive summaries do not compare qualified malignant-cell populations, prove antigen loss, or establish a cause of resistance.

## Completed live investigation

Run `a01769d0494a48fe84a2805208366b2e` was started through the actual website at 18:31:00 UTC and completed at 18:40:09 UTC on 19 September 2026. Its 28 real model calls used 247,950 input and 22,319 output tokens, with 35 recorded tool calls. Provider and worker policies were advisory; no threshold warning was needed at the configured 400,000-input threshold. The old 180,000-input stop would have prevented this run from completing.

Seven specialist products, coordinator synthesis and independent review passed their software contracts. These products include explicit blocked and inconclusive statuses; pharmacology used a deterministic missing-exposure gate without model inference. The reviewer requested a concrete barcode-linkage diagnostic, consumed its newly accepted result and revised the final recommendation. The final research decision remains **inconclusive**: unmatched barcodes must be excluded or resolved before variant attribution; the supplied data do not rank the original H1–H5 causal explanations. The target–effector crossover assay is a proposed experiment, not an executed one.

The original user-selected CD19 brief retained source name `User-selected CD19 example: CD19_CAR_T_.txt` and SHA-256 `03918c91a6905f28249ae9d4b5069548e5b18d10e5543a11c6293a8bea395a88`. Versioned skill receipts include the BioNeMo Boltz2 skill for molecular science and independent review. That proves instruction use, while the separate NVIDIA engineering receipt proves the monomer service call. Neither establishes a case-specific matched CAR-T prediction.

The saved JSON and Markdown report are in the parent workspace's `outputs/team-tbd-live-run.json` and `outputs/team-tbd-live-report.md`. The completed run remains available at the private local tunnel URL `http://127.0.0.1:8081/?run=a01769d0494a48fe84a2805208366b2e` while the tunnel and Brev service are running.

## Historical failures retained

A subsequent bounded-discovery run, `f3d94dc7889c432380542d7467b525ed`, accepted three computed analyses and the bioinformatics handoff, then received an incomplete statistics response at the 3,200-token response cap. Its eight requests used 47,294 input and 7,118 output tokens. No final decision was issued. The revised provider allows a configurable 8,000 output tokens per specialist response by default, requests concise products and preserves incomplete-output budget failures explicitly.

The subsequent higher-output run `34d67c24810841e9b6e3e14231c5600b` completed all seven specialists and coordinator synthesis (eight accepted handoffs), but stopped during independent review at the shared observed input threshold. It retained 27 actual requests, 197,834 input and 18,762 output tokens. It has no issued decision. That run demonstrated the need to raise the whole-team input allowance separately from the per-response output cap.

The first broad live run is `9b79483501f64cc9a658c58f42ef5221`: six real Astra model requests, 163,644 observed input tokens and 2,408 output tokens. Its accepted partial evidence and request receipts remain available. This exposed a real need for bounded exploration, smaller tool responses and capacity reserved for downstream specialists; later checks must be recorded separately rather than relabeling this run successful.

## What the automated checks establish

Provider integration tests exercise actual OpenAI `Agent`/`Runner` loops with mocked HTTP transports. They validate tool dispatch, nine-role handoffs, evidence references, applied skill receipts, independent review, selected-model identity and usage accounting. They do not establish vendor access. Separate actual request receipts above establish the Astra connection.

Data-reader checks cover source hash changes even when filesystem metadata stays constant, safe archive members and traversal, sparse dimensions and counts, HDF5 links/annotations, supported modality, input bounds and excluded formats. The Brev deployment exposed timestamp-only caching as insufficient; the implementation now hashes selected source bytes before trusting them.

Harness checks cover exact hypothesis preservation, source version and citation acceptance, immutable decisions, idempotency, correction and measurement lineage, missing molecular inputs, incomplete external work, cancellation, recovery, scoped skill integrity and reviewed procedural-memory release. NIM adapter tests use labeled artificial transport/structure fixtures; they do not demonstrate a real NVIDIA prediction.

## Required integration acceptance still open

- GPT-Rosalind itself requires API entitlement. Astra high is the user-authorized temporary placeholder, and every actual result must retain its returned model identity.
- NVIDIA service inference is now demonstrated with an actual official monomer sample. A qualified CAR-T molecular comparison still needs exact constructs, target-retention evidence and validated matched predictions; the service check alone does not establish that branch or test a resistance mechanism.
- Scientific review, qualified patient/sample mapping and experimental validation remain separate from software acceptance. Model-led exploration suggests competing explanations and discriminating measurements; it does not establish causality by itself.

The source catalog does not imply every indexed file was analyzed. Large RDS objects, copy-number-specific HDF5, unsupported modalities and files exceeding explicit bounds require additional adapters. Docker Compose is supplied but untested in this session; the verified service uses an isolated Python environment. Unknown asynchronous vendor jobs are preserved without automatic resubmission or reconciliation.


The user subsequently requested advisory spending budgets. The default now records and displays threshold warnings without stopping a run; enforced mode remains explicit. Regression checks verify continuation, one warning per category, frozen operation settings and required scientific gates. Historical exhausted runs above retain their original outcomes.


## General preparation and analysis skill update — 20 September 2026

Bioinformatics and statistics instruction versions advance to 1.2.0; discovery-planning advances to 1.1.0. The update recommends question- and design-dependent preparation/analysis, protects missingness and biological units, and separates evaluator references from independent evaluation. It adds no study-specific expected counts, thresholds, exclusions or new executable analysis. Data-analysis documentation now provides primary paper, deposit, code-release and access references.

Validation in an isolated application copy: all six skill integrity/receipt tests passed; the full suite passed (296 tests, one existing Starlette/AnyIO deprecation warning). A separate agent applied the guidance to an unrelated repeated-measure RNA/protein scenario without imposing study-specific rules or claiming execution. The role files use the application's versioned Markdown schema; their native manifest/load checks are authoritative rather than the standalone Codex SKILL.md schema. No new tests merely assert wording. No vendor/model calls, data transformations or new analysis recipes were run.


## Agent profiles — 20 September 2026

Static profiles for nine agents and 16 pinned skills were published on GitHub branch `docs/agent-profiles`, commit `7f8e31a6fcfe6583e1e2dfef069f296d841b77ed`, and deployed to the existing Brev workbench under `/static/agent-profiles/`, linked from navigation. All seven audited policy-source hashes matched Brev. Validation: 13 profile tests passed locally and against Brev source; the full local harness suite passed 776 tests. Browser checks covered search, filters, disclosures and desktop/mobile layouts. Read-only static routes passed an isolated application smoke test. The idle Brev service was stopped cleanly, updated and restarted; every served asset matched the release hash, the worker was healthy, and zero runs were active. No inference was requested. Credentials and existing runtime state were retained; private rollback and deployment receipts were saved on Brev.
