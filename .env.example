# Frontend deployment references only. Export these variables in the launching shell.
# Backend keys/settings belong in scientific_backend/.env; see its .env.example.
TEAM_TBD_BACKEND_URL=http://127.0.0.1:8080
TRACE_UI_PORT=8505
# Optional existing sealed private capsule, never committed:
# TEAM_TBD_CAPSULE=/absolute/path/to/team-tbd-results-capsule-2026-09-20-v1
# Optional existing historical service tunnels:
# TEAM_TBD_MAIN_URL=http://127.0.0.1:8081
# TEAM_TBD_ANA_URL=http://127.0.0.1:8082
# The old mock/investigation configuration is .env.legacy.example.
