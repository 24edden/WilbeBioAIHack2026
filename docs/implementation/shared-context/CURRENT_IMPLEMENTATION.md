# Current implementation boundary

Status snapshot prepared 19 September 2026 from the local `rosalind/docs/IMPLEMENTATION.md` record. This is context, not a fresh execution report or a claim that the code is installed on the shared CPU.

The local prototype implements question/tool contracts, input/output validation, evidence events and hashed artifacts, Evo 2 and paired Boltz-2 adapters, synthetic offline demonstrations, and an unresolved decision with a proposed next experiment. The implementation record reports 20 passing tests using offline fixtures and a loopback fake service. No successful live NVIDIA inference was claimed in that record.

The broader multidisciplinary debate, persistent scientist-feedback learning, approved lesson retrieval, and held-out generalization evaluation are proposed architecture. The new learning-loop document defines how to build and assess them; documentation does not make these capabilities operational.

The prototype does not yet establish a validated resistance mechanism or a patient finding. It lacks the blinded biological benchmark and several ingestion/analysis capabilities described in the architecture. Synthetic examples are demonstrations of plumbing, not biological ground truth.

This shared context bundle contains documents. It does not install the prototype, run analyses, alter the team's environment, or copy credentials or patient-level data. Confirm the team's actual code checkout and runtime before using implementation instructions.
