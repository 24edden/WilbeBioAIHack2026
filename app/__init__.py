"""Patient failure-investigation agent system — backend.

See ARCHITECTURE.md. Two seams carry the design: the provider interface
(`app.providers`) and the event bus (`app.events`).
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
